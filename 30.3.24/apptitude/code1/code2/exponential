#include <stdio.h>
int main()
{
    int b,e,res=1,ce;
    printf("Enter the base and exponent values:");
    scanf("%d %d",&b,&e);
    ce=e;
    while(e>0)
    {
        res=res*b;
        e--;
    }
    printf("%d power %d is:%d",b,ce,res);
    return 0;
}
